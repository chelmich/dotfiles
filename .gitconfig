[user]
    # These are stored in ~/.gitconfig_local

[include]
    path = ~/.gitconfig_local

[core]
    editor = nvim

[alias]
    lg = log "--pretty=format:'%C(auto,yellow)%h%C(auto,magenta)% G? %C(auto,blue)%>(12,trunc)%ad %C(auto,green)%aN %C(auto,reset)%s%C(auto,red)% gD% D' --date=relative"
